services:
  mikrotik-btest-exporter:
    environment:
      - API_USER=admin
      - API_PASS=secret_password
      - API_PORT=8728
      - EXPORTER_PORT=9115
      - TEST_DURATION=10
    ports:
      - "9115:9115"
